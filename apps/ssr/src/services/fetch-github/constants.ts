export const MOCADA = ['andrelarocca', 'philipeatela', 'aalphonsos']

export const NATA_REPOS_URL = '/orgs/natahouse/repos'
export const USER_URL = (user: string): string => `/users/${user}`
export const REPO_URL = (owner: string, repo: string): string =>
	`repos/${owner}/${repo}`
