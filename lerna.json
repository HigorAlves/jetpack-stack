{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "packages": ["apps/*", "packages/*", "shared/*", "docs"],
  "command": {
    "publish": {
      "conventionalCommits": true
    },
    "version": {
      "message": "build: new release version"
    }
  }
}
