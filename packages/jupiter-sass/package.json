{
  "name": "@jetpack/jupiter-sass",
  "version": "0.0.3",
  "description": "We believe that a good user experience is built with consistent design, well defined visual and interaction patterns, carefully built emotions and made for all.",
  "author": "Higor Alves",
  "license": "MIT",
  "main": "dist/index.css",
  "style": "dist/index.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist",
    "build": "sass src/main.scss:dist/index.css",
    "dev": "sass --watch src:dist",
    "postdev": "rimraf dist",
    "prepublish": "rimraf dist",
    "publish": "npm publish --access public"
  },
  "devDependencies": {
    "sass": "^1.30.0"
  }
}
